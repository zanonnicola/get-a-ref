# Migration `20201006184859-add-link-company`

This migration has been generated by Nicola Zanon at 10/6/2020, 8:48:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Link" (
"id" SERIAL,
"userId" integer   NOT NULL ,
"url" text   NOT NULL ,
"flagged" boolean   NOT NULL DEFAULT false,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"active" boolean   NOT NULL DEFAULT true,
"companyId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Company" (
"id" SERIAL,
"name" text   NOT NULL ,
"url" text   NOT NULL ,
"logo" text   ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Link.userId_unique" ON "public"."Link"("userId")

ALTER TABLE "public"."Link" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Link" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200929164338-add-times..20201006184859-add-link-company
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -17,8 +17,9 @@
   createdAt DateTime  @default(now())
   deletedAt DateTime?
   active    Boolean   @default(true)
   Profile   Profile
+  Link      Link[]
 }
 model Profile {
   id        Int       @default(autoincrement()) @id
@@ -28,15 +29,23 @@
   userName  String?
   userId    Int
 }
-// model Link {
-//   id Int      @default(autoincrement()) @id
-//   user User   @relation(fields: [userId], references: [id])
-//   userId Int     @unique
-//   Company Company
+model Link {
+  id        Int      @default(autoincrement()) @id
+  user      User     @relation(fields: [userId], references: [id])
+  userId    Int      @unique
+  Company   Company  @relation(fields: [companyId], references: [id])
+  url       String
+  flagged   Boolean  @default(false)
+  createdAt DateTime @default(now())
+  active    Boolean  @default(true)
+  companyId Int
+}
-// }
-
-// model Company {
-// id Int      @default(autoincrement()) @id
-// }
+model Company {
+  id   Int     @default(autoincrement()) @id
+  name String
+  url  String
+  logo String?
+  Link Link[]
+}
```


